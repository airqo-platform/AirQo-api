app:
  name: airqo-beacon-api
  label: beacon-api
  azureSecret: prod-env-beacon
  k8sSecret: prod-beacon-api
  namespace: production
replicaCount: 3
image:
  repository: airqoacr.azurecr.io/airqo-beacon-api
  tag: prod-b1e15408-1773067011
resources:
  limits:
    cpu: 400m
    memory: 600Mi
  requests:
    cpu: 50m
    memory: 250Mi
volumeMounts:
  - name: config-volume
    mountPath: /etc/config
volumes:
  - name: config-volume
    configMap:
      name: beacon-config-files
ingress:
  enabled: false
service:
  type: NodePort
  port: 8000
  protocol: TCP
  targetPort: 8000
  nodePort: 30115
autoscaling:
  minReplicas: 1
  maxReplicas: 3
  targetMemoryUtilizationPercentage: 70
