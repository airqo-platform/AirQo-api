nameOverride: ''
fullnameOverride: ''
images:
  repositories:
    api: airqoacr.azurecr.io/airqo-analytics-api
  tag: prod-2c64229c-1779108339
externalSecret:
  enabled: true
api:
  name: airqo-analytics-api
  label: analytics-api
  secret: prod-env-analytics
  replicaCount: 2
  podAnnotations: {}
  resources:
    common:
      limits:
        cpu: 200m
        memory: 2000Mi
      requests:
        cpu: 50m
        memory: 1400Mi
volumeMounts:
  - name: config-volume
    mountPath: /etc/config
volumes:
  - name: config-volume
    configMap:
      name: analytics-config-files
service:
  type: NodePort
  ports:
    - name: analytics-api
      port: 5000
      protocol: TCP
      targetPort: 5000
      nodePort: 30003
ingress:
  enabled: false
autoscaling:
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 75
