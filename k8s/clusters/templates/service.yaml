apiVersion: v1
kind: Service
metadata:
  name: {{ include "clusters.fullname" . }}
  labels:
    {{- include "clusters.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "clusters.selectorLabels" . | nindent 4 }}
