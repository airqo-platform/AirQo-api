FROM confluentinc/cp-schema-registry

ARG BOOTSTRAP_SERVERS=127.0.0.1:9092
ARG HOST_NAME=schema-registry

ENV SCHEMA_REGISTRY_HOST_NAME=$HOST_NAME
ENV SCHEMA_REGISTRY_KAFKASTORE_BOOTSTRAP_SERVERS=$BOOTSTRAP_SERVERS