apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.messageBroker.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "view.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
        app: {{ .Values.messageBroker.label }}
  replicas: {{ .Values.messageBroker.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  minReadySeconds: 10
  template:
    metadata:
      {{- with .Values.messageBroker.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        app: {{ .Values.messageBroker.label }}
    spec:
      affinity:
        nodeAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 90
              preference:
                matchExpressions:
                  - key: node-type
                    operator: In
                    values:
                      - high-memory
            - weight: 1
              preference:
                matchExpressions:
                  - key: node-type
                    operator: In
                    values:
                      - general-purpose
      containers:
        - name: {{ .Values.messageBroker.label }}
          image: "{{ .Values.images.repositories.messageBroker }}:{{ .Values.images.tag }}"
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: {{ .Values.messageBroker.configmap }}
          resources:
            {{- toYaml .Values.messageBroker.resources | nindent 12 }}
          volumeMounts:
            - name: config-volume
              mountPath: /etc/config        
      volumes:
        - name: config-volume
          configMap:
            name: view-api-config-files
