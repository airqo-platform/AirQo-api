apiVersion: v1
kind: Service
metadata:
  name: {{ include "batch-streams.fullname" . }}
  labels:
    {{- include "batch-streams.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "batch-streams.selectorLabels" . | nindent 4 }}
