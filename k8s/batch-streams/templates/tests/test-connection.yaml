apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "batch-streams.fullname" . }}-test-connection"
  labels:
    {{- include "batch-streams.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "batch-streams.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
