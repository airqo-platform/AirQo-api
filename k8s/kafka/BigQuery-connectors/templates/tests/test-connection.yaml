apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "BigQuery-connectors.fullname" . }}-test-connection"
  labels:
    {{- include "BigQuery-connectors.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "BigQuery-connectors.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
