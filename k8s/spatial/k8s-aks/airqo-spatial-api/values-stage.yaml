app:
  name: airqo-stage-spatial-api
  label: sta-spatial-api
  secret: sta-env-spatial
  namespace: staging
replicaCount: 2
image:
  repository: airqoacr.azurecr.io/airqo-stage-spatial-api
  tag: stage-d46631d7-1772989600
resources:
  limits:
    cpu: 200m
    memory: 500Mi
  requests:
    cpu: 10m
    memory: 180Mi
volumeMounts:
  - name: config-volume
    mountPath: /etc/config
volumes:
  - name: config-volume
    configMap:
      name: stage-spatial-api-config-files
ingress:
  enabled: false
service:
  type: NodePort
  port: 5000
  protocol: TCP
  targetPort: 5000
  nodePort: 31019
autoscaling:
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 80
