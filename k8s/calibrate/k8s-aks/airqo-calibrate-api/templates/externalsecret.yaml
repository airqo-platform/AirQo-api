apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.app.secret }}
  labels:
    {{- include "airqo-calibrate-api.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-kv-store   # your existing ClusterSecretStore
    kind: ClusterSecretStore
  target:
    name: {{ .Values.app.secret }}
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: {{ .Values.app.secret }} # key in external store
        version: latest