app:
  name: prod-gpmodel
  namespace: production
  configmap: env-gp-model-production

affinity:
  nodeAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 10
        preference:
          matchExpressions:
            - key: node-type
              operator: In
              values:
                - compute

image:
  repository: gcr.io/airqo-250220/airqo-gp-model-job
  pullPolicy: Always
  tag: latest

nameOverride: ""
fullnameOverride: ""

resources:
  limits:
    cpu: 2000m
    memory: 4000Mi
  # requests:
  #   cpu: 20m
  #   memory: 100Mi
