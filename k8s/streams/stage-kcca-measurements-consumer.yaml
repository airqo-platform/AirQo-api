###########################################################
# Start deployment of measurements consumer - staging
###########################################################
apiVersion: apps/v1
kind: Deployment
metadata:
  name: stage-kcca-measurements-consumer
  namespace: staging
spec:
  selector:
    matchLabels:
      app: stage-kcca-measurements-consumer
  replicas: 1
  minReadySeconds: 10
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: stage-kcca-measurements-consumer
    spec:
      containers:
        - name: stage-kcca-measurements-consumer
          image: us.gcr.io/airqo-250220/stage-stage-measurements-consumer:latest
          imagePullPolicy: Always
          resources:
            limits:
              memory: 512Mi
              cpu: "1"
            requests:
              memory: 256Mi
              cpu: "0.2"
          env:
            - name: SCHEMA_REGISTRY_URL
              value: http://stage-schema-registry-svc:8081
            - name: BOOTSTRAP_SERVERS
              value: stage-kafka-cluster-kafka-bootstrap:9092
            - name: INPUT_TOPIC
              value: kcca-streams-transformed-device-measurements-topic
            - name: BASE_URL
              value: http://staging-platform.airqo.net/api/v1/
            - name: CONSUMER_GROUP
              value: kcca-measurements-consumer-group
