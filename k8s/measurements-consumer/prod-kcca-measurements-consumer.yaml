apiVersion: apps/v1
kind: Deployment
metadata:
  name: prod-kcca-measurements-consumer
  namespace: production
spec:
  minReadySeconds: 10
  replicas: 1
  selector:
    matchLabels:
      app: prod-kcca-measurements-consumer
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: prod-kcca-measurements-consumer
    spec:
      containers:
      - name: prod-kcca-measurements-consumer
        image: us.gcr.io/airqo-250220/prod-measurements-consumer:latest
        imagePullPolicy: Always
        resources:
          requests:
            memory: 256Mi
            cpu: "0.2"
          limits:
            memory: 512Mi
            cpu: "1"
        envFrom:
          - configMapRef:
              name: prod-kcca-measurements-consumer-config
