apiVersion: v1
kind: Service
metadata:
  name: {{ include "measurements-consumer.fullname" . }}
  labels:
    {{- include "measurements-consumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "measurements-consumer.selectorLabels" . | nindent 4 }}
