app:
  name: airqo-stage-device-registry-api
  label: sta-device-reg
  secret: sta-env-device-registry
  namespace: staging
replicaCount: 2
image:
  repository: airqoacr.azurecr.io/airqo-stage-device-registry-api
  tag: stage-c06d4e6e-1774563553
resources:
  limits:
    cpu: 300m
    memory: 500Mi
  requests:
    cpu: 10m
    memory: 180Mi
volumeMounts:
  - name: config-volume
    mountPath: /etc/config
volumes:
  - name: config-volume
    configMap:
      name: device-registry-config-files
ingress:
  enabled: false
service:
  type: NodePort
  port: 3000
  protocol: TCP
  targetPort: 3000
  nodePort: 31002
autoscaling:
  minReplicas: 1
  maxReplicas: 2
  targetMemoryUtilizationPercentage: 80
