const join = {
  list: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
  updateUser: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
  deleteUser: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
  createUser: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
  updateUserDefaults: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
  getUserDefaults: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  confirmEmail: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  loginUser: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  forgotPassword: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  updateForgottenPassword: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  updateKnownPassword: () => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },

  verifyPasswordResetToken: (tenant, token) => {
    try {
    } catch (e) {
      logElement("server error", e.message);
    }
  },
};

module.exports = join;
