"use-strict";
const componentController = require("../Component");

let chai = require("chai"),
  expect = chai.expect;
chai.should();

describe("create a component", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should have validation error for a duplicate compoment type name", function() {});
});

describe("read component ", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should find the existing component ", function() {});
});

describe("deleting component ", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should delete a component using ID", function() {});

  it("should delete a component using the name", function() {});

  it("should remove multiple component ", function() {});

  it("should remove component using its instance", function() {});
});

describe("updating a component", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should set and saves a component using an instance", function() {});

  it("should update a component using its instance", function() {});

  it("should update one component using the model", function() {});

  it("should update one component with ID using model", function() {});

  it("should return error is the update action fails", function() {});
});
