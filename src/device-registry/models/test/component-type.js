"use-strict";
const componentTypeController = require("../ComponentType");

let chai = require("chai"),
  expect = chai.expect;
chai.should();

describe("create a component type", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should have validation error for a duplicate compoment type name", function() {});
});

describe("read component types", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should find the existing component types", function() {});
});

describe("deleting component types", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should delete a component type using ID", function() {});

  it("should delete a component type using the name", function() {});

  it("should remove multiple component types", function() {});

  it("should remove component type using its instance", function() {});
});

describe("updating a component type", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should set and saves a component type using an instance", function() {});

  it("should update a component type using its instance", function() {});

  it("should update one component type using the model", function() {});

  it("should update one component type with ID using model", function() {});

  it("should return error is the update action fails", function() {});
});
