"use-strict";
const createEventController = require("../create-event");

let chai = require("chai"),
  expect = chai.expect;
chai.should();

describe("list all devices", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should list all the devices on the platform", function() {});
});

describe("create a device", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should create a device", function() {});
});

describe("delete a device", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should delete a device", function() {});
});

describe("clear a device's readings", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should clear the readings of a device", function() {});
});

describe("update device's settings", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should update the settings of a device", function() {});
});

describe("list all devices by location", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should list all devices by location", function() {});
});
