"use-strict";
const imageController = require("../process-image");

let chai = require("chai"),
  expect = chai.expect;
chai.should();

describe("upload many images", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should upload many images", function() {});
});
