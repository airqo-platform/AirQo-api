"use-strict";
let chai = require("chai"),
  expect = chai.expect;
chai.should();

describe("transform events", function() {
  beforeEach(function() {});
  afterEach(function() {});
  it("should return the transformed events", function() {});
});
